# ── Build outputs ───────────────────────────────────────────────────────
build/
build-trace/
build-beta/
build-release/
build-release2/
build-f/
build_ninja/
build-cosim/
build-cosim-release/
build-cosim-release2/
cmake-build-*/
out/

# ── Prebuilt release/beta bundles (executables + windowed captures) ─────
1.Release/
2.Beta/
*build*/capture_battle/

# ── Generated recompiler output (regenerated by v2_regen) ──────────────
generated/

# ── Dependencies / third-party vendor folders ──────────────────────────
deps/
third_party/
snesrecomp-tool/recompiler-rs/target/

# ── Binaries / object files ─────────────────────────────────────────────
*.exe
*.dll
*.obj
*.o
*.a
*.lib
*.pdb
*.ilk
*.exp
*.so
*.dylib

# ── ROM / copyrighted game data ─────────────────────────────────────────
*.sfc
*.smc
*.bsv
*.smv
*.bst
*.srm
*.rtc
*.ips
*.gba
*.nds

# ── Traces / logs / captures (can be several GB) ───────────────────────
*.log
*trace*.log
*trace*.txt
*.jsonl
*.dmp
*.raw
ppu_log*.txt

# ── Intermediate images / dumps ─────────────────────────────────────────
*.ppm
*.raw
*.bin.dump
*.bmp
generated.bak/

# ── Tier2 / oracle capture logs (diagnostic dumps, not source) ─────────
tier2_so_*.json
tier2_so_*.bin
*.json

# ── Runtime user config / save states ──────────────────────────────────
config.ini
keybinds.ini
*.cfg.local
rom.cfg
project.txt
saves/
*.st
build_output.txt
sdd1_engine_out.txt
cosim_recomp_full2_done.txt
log_vram_*.txt
f_capture_restored.png

# ── Python caches / venv ────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# ── Rust build ───────────────────────────────────────────────────────────
recompiler-rs/target/
**/Cargo.lock.tmp

# ── Editor / OS ──────────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── Freebuff workspace ──────────────────────────────────────────────────
.freebuff/
# Nested repo metadata (upstream snesrecomp-tool moved .git backup)
snesrecomp-tool/.git_upstream_backup/
snesrecomp-tool/.gitmodules


# Diagnostic capture dirs (not source)
cosim/milestones_run2/
cosim/shipref/
cosim/view/
bg3_render.png

sanitize_paths.py
